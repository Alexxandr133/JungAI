# DESIGN_BRIEF.md — публичные лендинги JungAI

Версия 0.1 · август 2026 · владелец: фаундер
Статус: спецификация для реализации (в т.ч. Cursor)

## 0. Scope и как работать с этим файлом

**В scope:**
1. Лендинг «Для психологов» — маршрут `/for-psychologists` (делаем ПЕРВЫМ).
2. Лендинг «Для клиентов» — маршрут `/` (делаем ВТОРЫМ, заменяет текущий `GuestWorkspace`).
3. `/for-researchers` — отдельный лендинг (заменяет решение «блоком на /about»).
4. Карточка исследователя на `/about` и в футерах ведёт на `/for-researchers`.

**ВНЕ scope:** все кабинетные экраны (психолог/клиент/исследователь/админ), включая тёмные «мистические» экраны — их владелец оформляет сам и прописывает отдельно.

**Правила работы (в т.ч. для Cursor):**
- Файл — спецификация. Создать `.cursor/rules/landing.mdc` со ссылкой на этот файл и файлами копирайтов.
- Один заход в Cursor = одна секция (имена компонентов — в §6–7). Запрещено «переделывать всю страницу сразу».
- Токены (§4) не менять без явного решения владельца.
- Тексты брать ТОЛЬКО из `COPY-PSY.md` / `COPY-CLIENT.md` / `COPY-RESEARCH.md`. Если текста нет — стоп и вопрос, не выдумывать.
- Стили лендингов изолированы: собственный `frontend/src/styles/landing-tokens.css`, всё под scope-классом `.landing`. Глобальные `tokens.css`/`appearance.css` кабинетов не трогать.
- После каждой секции: сборка + скриншоты 1440px и 390px + сверка с чек-листом (§10).

## 1. Цель, тон, принципы

**Цели конверсии:**
- Лендинг психолога → регистрация специалиста → триал.
- Лендинг клиента → квиз/каталог → запись к психологу → регистрация.

**Тон:** дружелюбный, тёплый, «на равных». Мы не клиника (не холодные) и не эзотерика (не туманные). Мы — спокойный профессиональный сервис, который рад человеку.

**Три опоры дизайна:**
1. **Тепло** — светлая тёплая «бумага», серифный дисплейный шрифт, живые фото, мягкие формы.
2. **Ясность** — простые слова, открытые цены, понятные шаги, много воздуха.
3. **Доверие** — верификация, цифры, юридический контур (ИП, РКН № 15-26-005049, 152-ФЗ) в каждом ключевом блоке.

**Дизайн-референс по архитектуре:** ЯСНО (yasno.live / work.yasno.live/psychologist) — разделение воронок, цифры и цена в hero, верификация как маркер качества. Визуально НЕ копировать.

## 2. Запрещено (анти-паттерны текущей версии)

- Тёмный/мистический стиль на лендингах (резерв только для кабинетных экранов владельца).
- Градиенты на кнопках; glassmorphism; blur-карточки; shimmer-hover; «aurora»-блобы.
- Сетка из 3+ одинаковых карточек подряд (дампом фич — нет; чередовать раскладку).
- Внутренний жаргон в копирайте: «экосистема», «модальность» без пояснения, «интеллектуальный партнёр».
- Бейджи «демо» и фейковые данные на витрине; lorem ipsum; фейковые отзывы с выдуманными именами.
- Системный шрифт; скрытые цены («узнайте у менеджера»); CTA только «Личный кабинет».
- H1-«самоназвание» без выгоды для посетителя.

## 3. Типографика

Подключение: Google Fonts, `display=swap`, кириллица обязательна у обоих.

| Роль | Шрифт | Начертания | Применение |
|---|---|---|---|
| Дисплейный | **Lora** | 500/600/700 + italic | H1/H2, крупные цифры, эмоциональные акценты курсивом |
| Текстовый/UI | **Golos Text** | 400/500/600 | всё остальное |

Шкала (desktop → mobile через clamp):

| Элемент | Размер | Line-height | Шрифт |
|---|---|---|---|
| H1 | clamp(38px, 5.2vw, 60px) | 1.08 | Lora 600 |
| H2 | clamp(28px, 3.6vw, 40px) | 1.15 | Lora 600 |
| H3 | 20–22px | 1.3 | Golos 600 |
| Lead | 18–20px | 1.55 | Golos 400, `--ink-soft` |
| Body | 16px | 1.6 | Golos 400 |
| Small | 14px | 1.5 | Golos 400 |
| Eyebrow | 12px, uppercase, ls .08em | — | Golos 600, цвет `--brand` или `--sage` |

## 4. Токены (`landing-tokens.css`, scope `.landing`)

### 4.1 Токены — финал, палитра «mist»

```css
.landing {
  /* фоны */
  --paper:#F5F3FB;      /* страница */
  --paper-soft:#ECE9F8; /* чередующиеся секции, футер */
  --card:#FFFFFF;       /* якорь: карточки/инпуты чисто белые */

  /* текст */
  --ink:#211E2B; --ink-soft:#57536E; --ink-muted:#8A8496;
  --line:#E2DFF0;

  /* бренд и акценты (не меняются) */
  --brand:#6C5BD4; --brand-deep:#5546B8; --brand-soft:#EFEBFC;
  --peach:#F2A65A; --peach-soft:#FDEBDD;
  --sage:#3E8A6E;  --sage-soft:#E3F1EA;
  --data-cyan:#19E0FF; /* ТОЛЬКО data-viz */

  /* тени / контур карточек */
  --card-border:#D8D2EE;
  --shadow-card:0 1px 2px rgba(38,33,66,.05), 0 10px 28px rgba(38,33,66,.10);
  --shadow-card-hover:0 2px 4px rgba(38,33,66,.06), 0 14px 34px rgba(38,33,66,.13);
  --shadow-mock:0 24px 60px rgba(38,33,66,.12);
  --surface-2:#F3F1FA;

  /* геометрия (без изменений) */
  --radius-card:20px; --radius-input:12px; --radius-btn:999px;
  --container:1160px; --section-y:96px; /* 56px mobile */
}
```

**Кнопки:** primary — pill, solid `--brand`, белый текст, БЕЗ градиента; hover `--brand-deep`. secondary — outline 1.5px `--ink` на прозрачном. tertiary — текстовая со стрелкой →. Focus-ring 3px `--brand-soft`.
**Карточки:** `--card`, radius 20, border 1px `--card-border`, `--shadow-card`; интерактивным — hover `--shadow-card-hover`. Внутренние панели — `--surface-2` + border `--line`. Без blur/shimmer.
**Ритм секций:** чередование `--paper` / `--paper-soft`; каждая секция заканчивается CTA или связкой в следующую.
**Иконки:** lucide, 1.75px, rounded caps, цвет `--ink-soft`; в чипах — на подложках `--brand-soft`/`--peach-soft`/`--sage-soft`.

## 5. Визуальный язык

- **Иллюстрации:** мягкая плоская иллюстрация с зерном, тёплая палитра; мотивы — собственные символы платформы (вода, дом, лес, дорога, окно, дверь, свет, тень) из графика снов. Это наш уникальный визуальный словарь — не стоковые абстракции.
- **Фото:** живые тёплые портреты психологов в каталоге; тёплая цветокоррекция; никакого «корпоративного стока с рукопожатиями».
- **Моки продукта:** реальные скриншоты интерфейса в мягких рамках (`--shadow-mock`, лёгкий наклон 1–2°), с плавающими чипами-подписями («Клиент подтвердил сессию», «Транскрипция готова»). Никаких рисованных «телефонов с градиентом».
- **Data-viz:** график «Частота символов» сохраняем (преемственность), но только внутри белой карточки, градиент cyan→violet разрешён ТОЛЬКО здесь; бейдж «демо» убрать — при пустых данных блок скрывать.

## 6. Лендинг «Для психологов» — поблочная спецификация

Компоненты: `frontend/src/pages/landing/psychologist/*`, общие: `components/landing/LandingNavbar.tsx`, `LandingFooter.tsx`, `CtaBand.tsx`.

### S0 Navbar (`LandingNavbar`)
Логотип слева; якоря: Возможности · Исследования · Цены · Отзывы · FAQ; справа: «Войти» (ghost) + «Попробовать бесплатно» (primary). Sticky, фон `--paper`, hairline снизу при скролле.

### S1 Hero (`PsyHero`)
- Eyebrow: «Практикующим психологам и исследователям».
- H1: выгода, не самоназвание (текст — COPY-PSY.md).
- Lead: «вместо зоопарка Notion/Zoom/Calendly/ChatGPT — один контур: CRM, видео, календарь с самозаписью, ИИ под вашу модальность, транскрибация».
- CTA: «Попробовать бесплатно» + «Смотреть, как устроено» (якорь на S3).
- Trust-bar под CTA: живые счётчики (психологи/сессии/сны из `/public/stats`) + «Оператор ПДн — реестр РКН № 15-26-005049».
- Справа: мок экрана `/clients` или календаря + 2 плавающих чипа. Фон — `--paper` с мягкими органическими формами (арка/круг), НЕ aurora.

### S2 Боль «зоопарк» (`PsyPainZoo`)
Слева — «разбросанные» чипы инструментов (Notion, Zoom, Calendly, WhatsApp, ChatGPT, таблицы) с подписью «5 сервисов, 4 подписки, ничего не связано»; справа — одна карточка JungAI-контура. Связка в тур.

### S3 Продуктовый тур (`PsyTour`, блоки `PsyTourBlock`, zebra-раскладка)
6–7 блоков, каждый: eyebrow + H3 + 3–4 буллета ВЫГОД (не процессов) + мок + micro-CTA:
1. CRM и клиенты (карточки, теги, архив терапии, timeline).
2. Календарь и публичная самозапись клиента по ссылке.
3. Видеосессии + гостевая ссылка без аккаунта.
4. ИИ-ассистент с модальностью («ИИ не смешивает школы терапии») + вложения PDF/DOCX/фото.
5. Транскрибация сессий (спикеры, длинные записи).
6. Рабочая область и документы (Word-like, вставка из ИИ).
7. Публикации и сообщества = личный бренд и поток клиентов.

### S4 Исследовательский контур (`PsyResearch`)
«Больше, чем CRM»: дневник снов + AI-символы, проекты. Визуал — гексаграмма/график в data-viz стиле. Подпись-дифференциатор: «этого нет ни в одной CRM для психологов».

### S5 Шаги (`PsySteps`)
3 шага: регистрация 5 минут → верификация образования (2–3 дня) → практика: клиенты, календарь, контент. Крупные номера Lora.

### S6 Цены (`PsyPricing`)
3 тарифа: «Старт» 0 ₽ / «Практика» X ₽/мес (выделен, «выбирают 7 из 10») / «Практика + Исследования» Y ₽/мес. **Цифры утверждает владелец; цены открыты всегда, «свяжитесь с нами» запрещено.**

### S7 Отзывы (`PsyTestimonials`)
2–3 карточки: фото, имя, метод, цитата про время/рутину/рост. **Только реальные имена (ранние адоптеры, фаундер-интервью); нет реальных — секцию не публикуем.**

### S8 Верификация и этика (`PsyVerification`)
«Мы проверяем дипломы — поэтому клиентам спокойно у вас». Требования (профильное образование, метод 500+ часов, супервизия — по аналогии с ЯСНО) + блок данных: 152-ФЗ, РКН, «данные клиентов остаются в вашем контуре».

### S9 FAQ (`PsyFaq`, аккордеон)
6–8 вопросов: не юнгианцу подойдёт? / данные и ИИ / видео и гостевые ссылки / переезд с Notion и Zoom / тарифы и оплата (самозанятость, счёт) / верификация / мои ли клиенты остаются моими.

### S10 CTA-полоса (`CtaBand`)
Фон `--peach-soft`, H2 «Попробуйте платформу на этой неделе», primary + tertiary «написать: inbox@jung-ai.ru».

## 7. Лендинг «Для клиентов» — поблочная спецификация

Компоненты: `frontend/src/pages/landing/client/*`.

1. `CliHero` — H1 про «психолог, который слышит» + поддержка между сессиями; CTA «Подобрать психолога» (квиз 5–7 вопросов) + «Смотреть каталог»; trust-bar: верифицированы, сессия от X ₽, РКН.
2. `CliRequests` — чипы запросов (тревога, отношения, самооценка, утрата, выгорание, сны); клик → префильтр каталога/квиза.
3. `CliSteps` — 3 шага: квиз/подбор → встреча по видео на платформе → рост между сессиями (дневник, тесты).
4. `CliCatalog` — ЖИВЫЕ карточки из `GET /api/psychologists/public`: фото, метод, цена, «Записаться»; ссылка на полный каталог.
5. `CliBetween` — дифференциатор «терапия между сессиями»: дневник снов + AI-символы (здесь живой график, без «демо»), тесты (архетипы, индивидуация), wellness/прогресс; мок клиентского кабинета.
6. `CliTrust` — верификация специалистов, этика, 152-ФЗ/РКН, «ИИ не заменяет терапевта».
7. `CliTestimonials` — клиентские отзывы с согласия, формат «Анна, 34».
8. `CliFaq` + `CtaBand` + общий футер (документы, реквизиты ИП, РКН).

## 8. SEO/мета для лендингов (минимум)

- Per-route `<title>`/`description`/OG/Twitter + canonical (react-helmet-async или аналог).
- `/for-psychologists`: title «JungAI — платформа для психологов: CRM, видео, ИИ, транскрибация».
- `/`: title под запрос «юнгианский психолог онлайн» (финал — в COPY).
- `robots.txt` + `sitemap.xml` с публичными URL; OG-картинки 1200×630 в тёплой палитре.

## 9. Порядок реализации в Cursor

1. Положить `DESIGN_BRIEF.md`, создать `.cursor/rules/landing.mdc` (ссылка на brief + запрет трогать кабинетные токены).
2. Создать `COPY-PSY.md` / `COPY-CLIENT.md` (следующий шаг с ассистентом).
3. Папка `/design-references`: скриншоты yasno.live, work.yasno.live/psychologist + 2–3 визуальных референса (тёплые бренды: Zigmund.Online, Headspace-иллюстрации) — прикладывать в чат.
4. Реализация по секциям (§6–7), каждая — отдельным компонентом; после каждой — скриншоты 1440/390 и сверка с §10.
5. В конце: мета/OG (§8), прогон чек-листа, только потом деплой по ранбуку.

## 10. Чек-лист приёмки секции

- [ ] CTA или связка в следующую секцию присутствует; primary-CTA в первом экране.
- [ ] Тексты — дословно из COPY-файлов; без жаргона и «демо».
- [ ] Токены — только из §4; кнопки без градиента; нет blur/shimmer/aurora.
- [ ] Шрифты Lora/Golos с кириллицей; H1 влезает на 390px без горизонтального скролла.
- [ ] Моки — реальные скриншоты; фото — реальные или помечены как заглушки до замены.
- [ ] Цены видимы; trust-элемент (цифра/верификация/РКН) в hero и в S8/S6.
- [ ] Контраст текста ≥ WCAG AA; focus-ring виден с клавиатуры.

## 12. Публичный профиль психолога (`/psychologists/:id`)

**Оболочка:** маршрут — публичный, рендерится в landing-оболочке:
`LandingNavbar` (публичный вариант) + landing-футер, фон `--paper`, scope `.landing`.
Кабинетный навбар здесь НЕ показывается (если психолог смотрит свой профиль — тонкий баннер «Вы смотрите свой профиль глазами клиента»).

**Единый компонент:** `PsychologistPublicCard` — один источник правды.
Используется и на публичной странице, и в модалке предпросмотра в кабинете
(«Так профиль видит клиент»). Два рендера запрещены.

### 12.1 Шапка-карточка
- Карточка `--card`, radius 20, `--shadow-card`.
- Обложка: высота 180px desktop / 120px mobile, скругление только сверху;
  `object-fit: cover`. Текст на обложке ЗАПРЕЩЁН (любой).
  Нет обложки → дефолт: мягкий градиент `--peach-soft` → `--brand-soft`.
- Аватар 96px, кольцо 4px `--card`, наезжает на нижний край обложки
  (margin-top: -48px), слева 32px.
- Имя: Lora 600, 28–32px, `--ink` — НА БЕЛОЙ части карточки, справа от аватара
  (mobile — под аватаром). Рядом бейдж «Верифицирован» (`--sage-soft`/`--sage`).
- Под именем: метод + опыт (`--ink-soft`); цена «Сессия — X ₽» Golos 600 20px,
  цвет = акцент психолога; «Ближайшая запись: …» (`--ink-soft`, small).
- CTA «Записаться» (primary, цвет = акцент, pill, без градиента) — в шапке,
  якорь на расписание. Mobile: sticky-бар внизу экрана: цена + «Записаться».
- Строки «Отзывы: 0 · средняя оценка 0» НЕТ. Отзывы — только секцией ниже,
  и только если их ≥ 1.

### 12.2 Персонализация (предохранители)
- Акцент — только кураторская палитра из 8 тёмных свотчей (контраст с белым
  текстом гарантирован палитрой, не выбором пользователя).
- Акцент применяется ТОЛЬКО к: CTA «Записаться», выбранный слот, цена,
  ссылки-стрелки. Остальное — нейтральные токены §4.
- Обложка — только изображение (рекомендация в кабинете: 1600×480, ≤ 2 МБ).

### 12.3 Секции и порядок
1. Шапка-карточка (12.1).
2. «С чем работает» — чипы: `--paper-soft` фон, border `--line`, radius 999.
3. «О себе» — текст, `--ink-soft`, line-height 1.6.
4. «Образование и курсы» — карточки `--card` + hairline (не голубые из старой темы).
5. «Расписание и запись» — desktop: 2 колонки (слоты 60% / форма 40% sticky);
   mobile: столбиком.
6. «Отзывы» — только при наличии; карточки с именем-форматом «Анна, 34».
7. (v2) «Публикации психолога» — ссылки на статьи для экспертности.

### 12.4 Расписание и форма
- Заголовок дня: Golos 600. Слот: `--card`, border `--line`, radius 12;
  hover — border `--ink-muted`; выбранный — фон = акцент, белый текст.
- Инпуты: `--card`, border `--line`, radius 12, focus-ring 3px `--brand-soft`.
- Плейсхолдеры: «Ваше имя» · «Ваш email» · «Телефон (необязательно)» ·
  «Пара слов о запросе (необязательно)».
- Кнопка submit = акцент, pill. Под формой: «Подтверждение и детали встречи
  придут на почту».

  ## 13. Редактор профиля психолога (`/profile`)

Страница рендерится в landing-токенах (§4): тёплая бумага, белые карточки,
solid-кнопки. Градиенты запрещены (в т.ч. на чипах и кнопках сохранения).
Остальной кабинет не трогаем.

### 13.1 Раскладка
- Desktop: слева форма (≈60%), справа STICKY live-превью (≈40%, min 360px),
  рендерит `PsychologistPublicCard` (§12) с draft-данными в реальном времени.
- Mobile: плавающая кнопка «Предпросмотр» → bottom-sheet с тем же компонентом.
- Кнопка «Предпросмотр профиля» из шапки убирается (превью теперь всегда рядом).

### 13.2 Две зоны, явно разделённые
**Зона «Публичный профиль»** (главная, сверху): всё, что видит клиент.
Шапка зоны: «Это видят клиенты» + индикатор полноты (13.5).
Секции по порядку:
1. «Карточка в каталоге»: фото, ФИО, метод/подход, стоимость, опыт, теги.
2. «О себе и подходе»: текст «О себе», «С кем работаете».
3. «Образование и супервизия».
4. «Оформление»: обложка + акцентный цвет.
**Зона «Аккаунт»** (компактная карточка ниже): email (+смена), телефон,
пароль, уведомления. Статус верификации — рядом с аватаром в публичной зоне.

### 13.3 Теги «С чем работаете»
- Единый кураторский словарь ≈16 тегов в `shared/profileTags.ts` — один
  источник для редактора И для чипов клиентского лендинга (S2 = топ-8 словаря).
- UI: мульти-выбор чипами + поиск по словарю; выбранный чип = `--brand-soft`
  фон + `--brand` текст + border; крестик для снятия.
- Свободные теги через запятую УБРАТЬ (v2 — «свой тег» через модерацию).
- Минимум 3 тега для полноты.

### 13.4 Хелперы и микрокопирайты (дословно)
- Фото: «Квадратное фото с лицом, от 400×400. Таким вас видят в каталоге».
- Обложка: «Обложка — просто фон, текста на ней не будет. 1600×480, до 2 МБ».
- Акцент: «Акцент — это кнопка „Записаться“, выбранный слот и цена на вашей
  публичной странице».
- Метод: label «Метод / подход», placeholder «Например: юнгианская
  аналитическая психология».
- Стоимость: «Клиент видит цену до записи — в карточке и в профиле».
- О себе: «2–4 предложения: ваш подход, с чем работаете, как проходит первая
  встреча. Это первый текст, который читает клиент после имени» + счётчик 0/600.
- Образование: «Образование показывается клиентам — это маркер доверия».
- С кем работаете: на публичной странице выводится как
  «Формат работы: индивидуально · с парами · с детьми».

### 13.5 Полнота профиля (мягкий nudge, не гейт)
Прогресс «Заполнено N из 6» + чек-лист:
фото ≠ дефолт · обложка · «О себе» ≥ 200 символов · теги ≥ 3 ·
образование ≥ 1 · стоимость > 0.
Подпись: «Заполненные профили получают больше записей».

### 13.6 Сохранение
- Sticky-бар снизу при dirty-state: «Есть несохранённые изменения» +
  «Сохранить изменения» (solid `--brand`, pill) + «Отменить».
- Нижняя кнопка дублируется только этим баром; старого градиентного submit нет.

## 14. Анкета подбора (`/match`, CTA клиентского лендинга)

Оболочка: landing (LandingNavbar + футер, `--paper`).
Градиенты запрещены везде, включая «Далее»/«Показать».

### 14.1 Индикатор шагов
Чипы: пройден = `--sage-soft` + `--sage`; активен = `--brand-soft` + `--brand`;
впереди = `--paper-soft` + `--ink-muted`. Клик по пройденному шагу — возврат.

### 14.2 Шаги
- «Для кого» и «Бюджет»: карточки-варианты; selected = border 1.5px `--brand`
  + фон `--brand-soft`. Бюджетные якоря оставить (до 3 500 / 3 500–5 500 / от 5 500).
- «Тема»: чипы из `shared/profileTags.ts` (тот же словарь, что §13 и фильтры
  каталога); мульти-выбор ДО 3, счётчик «выбрано N из 3» + подсказка
  «до трёх — так рекомендация точнее».
- Свободный текст: оставить, 0/300, подсказка дословно: «Этот текст психолог
  увидит в заявке на запись — пишите как есть».
- «Время»: v1 НЕ жёсткий фильтр; сохраняется и уходит в префилл заявки.
- Кнопки: primary solid `--brand` pill; «Назад» — ghost.

### 14.3 Результаты («Подбор»)
- Заголовок: «Рекомендуем» + sub «N специалистов под ваш запрос».
- Карточки = `PsychologistMiniCard` (Тот же компонент, что в каталоге §15!):
  аватар с fallback (фото onError → буквенный аватар на `--brand-soft`;
  битых img быть не должно), имя, метод, опыт, теги (совпавшие с анкетой —
  подсвечены `--sage-soft`), цена «Сессия — X ₽», ближайший слот (если ≤ 7 дней),
  бейдж «Верифицирован», CTA «Профиль и запись» (solid `--brand`).
- Строка причины: «Совпадение: темы · формат · бюджет» — прозрачность доверия.
- Сортировка: по числу совпадений, затем `catalogSortOrder`.
- Показывать до 4 + ссылка «Смотреть весь каталог».
- Релаксация при 0: снять бюджет → снять формат; плашка «Мы расширили критерии
  поиска». Если всё равно 0: empty-state «Сейчас нет точного совпадения» +
  «Весь каталог» + «Пройти заново».
- Результат в `localStorage client_quiz_result`; на профиле психолога —
  префилл поля «Короткий комментарий»: «Запрос из анкеты: [темы] · [формат] ·
  [свободный текст]».

## 15. Каталог психологов (`/psychologists`)

Оболочка: landing (навбар с активным «Психологи», футер, `--paper`).

### 15.1 Шапка и тулбар
- H1 «Психологи» + sub «N верифицированных специалистов» (число живое).
- Поиск (инпут по §12.4) + фильтры: чипы запросов (`shared/profileTags.ts`,
  горизонтальный скролл на mobile), селекты «Формат» и «Стоимость» (3 бакета),
  «Сбросить фильтры» при активных.

### 15.2 Карточка `PsychologistMiniCard` (общая с анкетой)
- Equal-height grid: 3 / 2 / 1 колонки.
- Аватар 72px (фото или буквенный fallback), имя полностью (перенос, не «…»),
  метод + опыт, «О себе» line-clamp-2, теги топ-3 (+N), цена «Сессия — X ₽»,
  ближайший слот ≤ 7 дней (точка `--sage`), бейдж «Верифицирован».
- CTA «Профиль» (primary small); карточка кликабельна целиком.
- «Написать» — ТОЛЬКО для авторизованного клиента (secondary); гостю не показывать.
- Отзывы: строка «Рейтинг X · N отзывов» ТОЛЬКО при N ≥ 1. Никаких «0».

### 15.3 Данные и гигиена витрины
- Прямо сейчас через `/admin/psychologists-catalog` скрыть: тестовые аккаунты
  (Trofimov.*, «Тестовый психолог», дубли), пока не заполнят профили.
- Правило продукта: ноль-значения (отзывы, опыт, рейтинг) не рендерятся нигде.
- v2: авто-допуск в каталог при полноте профиля ≥ 4/6 (§13.5).

## 16. Лендинг «Для исследователя» (`/for-researchers`)

Компактный: 7 секций, без дампа фич и без цен (ранний доступ — плашкой).
Оболочка landing, токены §4.1 (mist).

### 16.1 Характер и акценты
- Единственная публичная страница, где `--data-cyan` разрешён как акцент
  «науки»: data-viz (график символов), гексаграмма, eyebrow у data-блоков,
  тонкие маркеры. Всё остальное — общая система (§3–§5).
- Hero-визуал: SVG-гексаграмма индивидуации (переиспользовать
  `IndividuationHexagram`) + мок графика частоты символов в белой карточке.
  Это «мистика + наука» одним взглядом, без тёмных экранов.

### 16.2 Секции
S1 Hero · S2 Тур из 4 блоков (Материал / Модель / Пространство / Выход) ·
S3 Мост «практикующему психологу» → `/for-psychologists` ·
S4 Этика и данные (анонимизация, 152-ФЗ, РКН) · S5 Шаги + плашка раннего
доступа · S6 FAQ · S7 CTA-полоса.
Копирайты — дословно из `COPY-RESEARCH.md`.

## 17. Кабинеты (психолог / клиент / исследователь / админ)

### 17.1 Темовая стратегия
- Дефолт — СВЕТЛАЯ тема из семьи лендингов (mist); тёмная остаётся
  переключателем («Тема») и обязательной для «мистических» экранов,
  которые владелец оформляет сам.
- Компоненты темонезависимы: всё через токены, никаких хардкодов цвета.

### 17.2 Токены кабинета (светлые; расширяют §4.1)
Те же --paper/--card/--line/--ink/--ink-soft/--brand/… плюс:
--danger:#D4574E; --danger-soft:#FBEAE8;
--warning:#B97F2E; --warning-soft:#FBEFDD;
--sage/--sage-soft — успех/верификация; --data-cyan — ТОЛЬКО данные/исследования.
/* контур карточек — темнее внутренних hairline */
--card-border:#D8D2EE;
/* слоёная тень: резкий край + мягкий подъём (на светлом фоне читается) */
--shadow-card:0 1px 2px rgba(38,33,66,.05), 0 10px 28px rgba(38,33,66,.10);
--shadow-card-hover:0 2px 4px rgba(38,33,66,.06), 0 14px 34px rgba(38,33,66,.13);
/* внутренние панели глубже, чтобы структура карточки читалась */
--surface-2:#F3F1FA;

### 17.3 Компоненты
- Кнопки: solid, без градиентов во всех темах; primary = --brand;
  destructive = --danger; ghost/outline — вторичные.
- Карточки: `--card`, border 1px `--card-border`, `--shadow-card`;
  интерактивным — hover `--shadow-card-hover`; без blur/shimmer.
  Внутренние панели: `--surface-2` + border `--line` (не как внешний контур).
  Тень у модалок/дропапов — тоже `--shadow-card` / `--shadow-card-hover`.
- Инпуты: по §12.4; селекты и даты — в том же стиле.
- Таблицы: zebra --surface-2, sticky-шапка, плотные строки 44–48px.
- Табы/фильтры/модалки/тосты — по одному компоненту на паттерн на все роли.

### 17.4 Типографика плотного UI
Golos Text везде; Lora — ТОЛЬКО H1 экрана и крупные empty-state.
Шкала: 13 (метки) / 14 (таблицы, вторичный) / 16 (body) / 20 (H3) / 28 (H1).

### 17.5 Шаблон страницы
Header: H1 (Lora) + sub (--ink-soft) + primary-действие СПРАВА.
Контент: сетка 8px; сайдбар/навбар — --surface-2, активный пункт --brand-soft.

### 17.6 Состояния
- Empty-state: иконка + Lora-заголовок + одна CTA (пустота = онбординг).
- Loading: скелетоны --paper-soft; Error: inline --danger + действие повтора.

### 17.7 Запрещено в кабинетах
Градиенты на кнопках/чипах; shimmer/blur; системный шрифт; голые «0»;
разные стили одних паттернов между ролями.

## 18. Экран «Сессии и встречи» (/events)

### 18.1 Каркас
- Header: H1 Lora + sub «Часовой пояс: Europe/Moscow»; справа:
  «Календарь» (secondary) + «Запланировать встречу» (primary solid).
- Под header: табы «Предстоящие · История» (вместо кнопки «История»)
  + справа чипы-фильтры типов БЕЗ дублей: Сессия · Видеовстреча ·
  Супервизия · Вебинар.
- Контент: grid 2/3 + 1/3.
  Левая колонка: карточка «Ближайшая» + список событий по датам.
  Правая колонка: «Публичный календарь» · «Требуют внимания» · «Быстрый звонок».

### 18.2 Карточка «Ближайшая»
Дата/время + «через N ч/дн» (живой countdown), клиент, бейдж типа,
статус (подтверждена/ожидает). Действия: «Комната» (primary; активна
за 15 минут до начала, иначе disabled с подсказкой), «Ссылка-приглашение»
(copy), «Перенести» (ghost).

### 18.3 Карточка события
[время–время] [бейдж типа] [бейдж статуса: подтверждена / ожидает /
заявка клиента] · заголовок · клиент. Действия: «Комната», «Ссылка»,
меню ⋯ (перенести, удалить с confirm). Красной кнопки на карточке нет.

### 18.4 Правая колонка
- «Публичный календарь»: статус «Запись по ссылке включена», ссылка + copy,
  «Открыть страницу записи», «Настройки доступности» (см. 18.6),
  микрокопия: «Клиенты записываются сами — вы подтверждаете».
- «Требуют внимания»: клиенты без сессий > 2 недель; кнопка у каждого
  «Предложить время» → модалка планирования с префиллом клиента.
- «Быстрый звонок»: «Создать комнату сейчас и получить ссылку» (терминология
  вместо голого «Позвонить»; зелёную кнопку из header убрать).

### 18.5 Модалка планирования
Добавить селект «Клиент (необязательно)» → после создания шлётся приглашение
(статус на карточке события). Итог-строка человеческая: «Видеовстреча ·
09:00 · дата не выбрана»; «Создать встречу» disabled до валидности.
Кнопки solid, без градиента.

### 18.6 Настройки доступности → BACKEND
Префы (рабочий день, обед, перерыв, шаг, выходные) хранить на backend
per-psychologist, чтобы МОЙ календарь и ПУБЛИЧНАЯ страница записи считали
окна по одним правилам (сейчас localStorage — расхождение).
Модалку стилизовать по §12.4/§17; длинную справку — под info-иконку.

### 18.7 Календарь (модалка)
Легенда: sage/warning/brand вместо стихийных цветов; «Поделиться календарём»
убрать из модалки (живёт в карточке 18.4); остальное сохранить.

### 18.8 Заявки и запросы (единый блок, вместо /requests)
- Маршрут /requests и пункт навигации удаляются. Дашборд-чип «N заявок»
  ведёт на /events#requests (скролл к блоку).
- Блок — верх левой колонки, над «Ближайшей». Оба типа заявок:
  «Заявка на слот» (публичный календарь) и «Заявка из подбора» (анкета),
  бейдж типа + «Новый» + дата получения.
- Карточка заявки: клиент (имя, email, телефон) + АНКЕТА СТРУКТУРОЙ,
  не сплошным текстом: «Для кого», «Темы» чипами, «Бюджет»,
  «Желаемый слот» (bold), «Комментарий» отдельной строкой.
- Действия: «Принять» (primary solid) · «Написать» (secondary; disabled
  для незарегистрированных с подсказкой) · «Отклонить» (ghost, модалка
  причины → чат/email).
- После «Принять»: заявка исчезает из блока, создаётся событие с бейджем
  «Первая встреча»; на карточке события с участником-не-клиентом — кнопка
  «В клиенты» (открывает модалку добавления клиента с префиллом из анкеты).
- После endsAt прошедшей первой встречи без добавления — мягкая плашка
  на событии: «Первая встреча прошла — добавить в клиенты?».

## 19. Pre-join («Готовы подключиться»)
Тёмный экран — преемственность с комнатой (комнату НЕ меняем).
- Центр: self-view превью камеры (выключена → аватар, как в комнате) +
  селекторы устройств (мик/камера) + тумблеры вкл/выкл.
- Гость: поле «Ваше имя» (авторизованному — префилл).
- Микрокопия доверия: «Встреча проходит на платформе JungAI, без сторонних
  сервисов. Камеру и микрофон можно выключить в любой момент».
- CTA: «Подключиться» (solid --brand, крупная) + secondary «Без камеры».
- Сверху строка встречи: «Сессия с [психолог] · сегодня, 18:00».

## 20. Публичная страница записи (/book/calendar)

- Минимальный публичный шелл: БЕЗ кабинетного навбара. Сверху: лого +
  «Запись к [Имя Фамилия]»; облегчённый футер (документы + РКН).
- Токены mist (§4.1) + читаемые карточки (card-border/shadow); H1 Lora.
- Host-шапка: фото/аватар, имя, метод, бейдж «Верифицирован»,
  «Сессия 50–60 минут · X ₽» — персонализация повышает конверсию.
- Индикатор шагов День→Слот→Данные по §14.1 (active brand, done sage).
- Календарь: легенда sage/warning/brand; «Далее» solid brand, disabled
  до выбора; градиентов нет.
- Шаг «Данные»: инпуты §12.4 + ОБЯЗАТЕЛЬНЫЙ чекбокс согласия на обработку
  ПДн (форма собирает имя/email/телефон — без согласия нельзя, у вас это
  зафиксировано в юридическом контуре); комментарий префиллом из
  client_quiz_result, если есть.
- Финал: success-state «Заявка отправлена. [Имя] подтвердит встречу —
  детали придут на почту» + «Вернуться к профилю».

  ## 21. Мессенджер психолог↔клиент
- Контейнеры: пузырь 56px fixed bottom-right (solid --brand, бейдж
  непрочитанных --peach) → drawer 400px справа (--card, border-left --line,
  --shadow-mock); /messages — full-mode тех же компонентов; mobile — drawer
  во весь экран.
- Список: аватар --brand-soft, имя, превью последнего + время, unread-бейдж;
  активный --brand-soft. «Новый чат» = + в шапке (модалка выбора клиента).
- Переписка: мои пузыри --brand/белый справа, чужие --surface-2 слева;
  radius 16 (угол 4 со стороны отправителя); время --ink-muted;
  статусы: ✓ доставлено (--ink-muted), ✓✓ прочитано (--sage); online-точка
  --sage у имени.
- Инпут §12.4; Enter — отправить, Shift+Enter — перенос; кнопка-иконка solid.
- «Удалить чат» — в меню ⋯ шапки с confirm.
- «Написать» на карточке/в профиле клиента открывает drawer с этим чатом.
- v2: сообщение попадает в Timeline клиента (связь с CRM).

## 22. Realtime мессенджера (техспека)
- Один app-level сокет (ChatSocketProvider), heartbeat 25s, reconnect
  backoff 1–30s + ресабскрайб; guard от StrictMode-дубля.
- Nginx app-WS: Upgrade-заголовки, read/send timeout 300s.
- Протокол: messageId + server-ack, read-receipt, unread с сервера, presence.
- Критерий приёмки: в логах ОДНО стабильное соединение на пользователя,
  без циклических connected/disconnected; навигация не рвёт сокет.
- socket.io — только если после этого протокол всё равно не стабилен.

## 23. Регистрация / вход / код подтверждения

### 23.1 Шелл
- Публичный: фон --paper (mist), без кабинетного навбара; лого слева → `/`;
  справа ссылка «Уже есть аккаунт? Войти» (на /login — «Регистрация»).
- Переключатель темы с карточки убрать: публичная авторизация всегда светлая.
- H1 Lora; sub --ink-soft «Создайте аккаунт…».

### 23.2 Раскладка (desktop 2 колонки, mobile 1)
- Слева (40%): брендовый блок — лого, ценностные буллеты ПО ВЫБРАННОЙ РОЛИ:
  клиент: «верифицированные психологи · дневник снов · тесты»;
  психолог: «CRM и календарь · ИИ под модальность · транскрибация»;
  исследователь: «AI-символы снов · индивидуация · проекты»;
  trust-строка: «Оператор ПДн — реестр РКН № 15-26-005049».
- Справа (60%): карточка формы (--card, --card-border, --shadow-card).

### 23.3 Форма
- «Роль» — НЕ native select: три карточки-сегмента (иконка + название +
  строка описания); selected = border --brand + --brand-soft; смена роли
  меняет буллеты левого блока.
- Секции с заголовками 12px uppercase --ink-muted: «Аккаунт» (email, пароль
  с show/hide, подтверждение), «О себе» (имя + role-specific поля), «Согласия».
- Инпуты §12.4; autofill перекрашивать в белый (-webkit-autofill shadow);
  autocomplete=email / new-password / name.
- Согласия: чекбоксы, текст 13px --ink-soft, ссылки --brand; юридические
  формулировки сохранить, визуальный вес снизить (сейчас простыня давит).
- CTA: solid --brand, full-width в карточке; градиент запрещён.
- Под CTA: «Уже есть аккаунт? Войти».

### 23.4 Шаг кода (после submit, в той же карточке)
«Мы отправили код на [email]» + 6-значный инпут + «Отправить повторно»
с таймером 60с + «Изменить почту» (назад). Не новая страница.

### 23.5 Компактная регистрация («квадрат», без скролла)
- Desktop: форма СЛЕВА (60%), брендовый блок СПРАВА (40%, vertical center).
  Mobile: форма первая, бренд-блок скрыт (или одна строка под карточкой).
- Карточка: max-width 640px; целевая высота ≤ ~700px — согласия и CTA
  видны на экране 1366×768 без скролла.
- Роль: НЕ три вертикальные карточки — горизонтальный сегмент-контрол
  из трёх (иконка + название); selected = --brand-soft + border --brand.
  Под сегментом — ОДНА строка-описание выбранной роли (меняется при
  переключении; это же чинит склейку «КлиентНайти психолога…»).
- Сетка полей внутри карточки:
  · Email — full width;
  · Пароль + Подтверждение — две колонки (show/hide в каждом);
  · Имя — full width;
  · Согласия — два чекбокса, 13px, line-height 1.45;
  · CTA solid --brand full width; под ним центрированная строка
    «Уже есть аккаунт? Войти» 13px.
- Уплотнение: заголовки секций 12px uppercase, вертикальные отступы
  между секциями 12–16px (вместо 24–32); sub под H1 убрать (дублирует).
- H1 Lora 24–28 внутри карточки.
- Шаг кода (23.4) заменяет содержимое той же карточки — карточка при
  этом становится ещё ниже, скролла нет.
- Брендовый блок справа: лого, H1 Lora «JungAI — пространство практики»,
  буллеты по роли, trust-строка РКН — как в 23.2, но справа.

  ## 24. Рабочая область психолога
### 24.1 Header
H1 Lora + селектор клиента (chip, аватар --brand-soft); справа: статус
автосохранения «Сохранено · HH:MM» (--sage, фикс-ширина без скачка),
«Сохранить сейчас» (secondary), info-тур.
### 24.2 Сайдбар табов
«×» только по hover; dbl-click — rename; «+» — меню «Пустая / Шаблоны
(Запрос, Анамнез, Сны, Ценности…)»; drag-reorder — v2.
### 24.3 Тулбар (v1, одна плоская панель)
[Стиль абзаца: Обычный/H2/H3 — styled select] · B I U S ·
[Highlight: 4 кураторских цвета (peach/sage/lavender/yellow soft)] ·
[Цитата] · [Чек-лист] · [•][1.] · [indent−][indent+] · [align L/C/R] ·
[link][unlink] · [Таблица ▾ 10×10][Удалить таблицу] · [undo][redo][clear] ·
[«ИИ по клиенту» — ghost primary в конце].
Убрать: селект шрифта, свободный цвет текста.
### 24.4 Редактор и статус-бар
Колонка 760px, --card, lh 1.7; внизу статус-бар: «N слов · M символов ·
Клиент: [имя] · Сохранено HH:MM».
### 24.5 ИИ-связка
«ИИ по клиенту» открывает ИИ-чат с clientMode и контекстом табов ТЕКУЩЕГО
клиента; в ИИ-чате — чипы табов, входящих в контекст. v2: боковая
ИИ-панель внутри области (как в проектах исследователя).
### 24.6 v2
Экспорт DOCX/PDF, история версий, drag-reorder табов.

## 25. ИИ-чат психолога
### 25.1 Модель контекста (единственная)
Контекст = модальность + клиент (опционально) + сны/заметки клиента
(только при выбранном клиенте, дефолт on). Чекбокс «Режим работы с
клиентами» удалён (производен от выбора клиента). В шапке чата — кликабельные
чипы контекста; смена через них или через сайдбар.
### 25.2 Сайдбар
[Новый чат solid] [Новая папка] · список чатов/папок · блок «Контекст ИИ»:
чип модальности (+изменить), селект клиента (кастомный, «Обобщённый режим»),
тумблер снов (disabled без клиента). Чат появляется в списке только после
первого отправленного сообщения.
### 25.3 Настройки (правый drawer, шестерёнка)
Квота текстом («0 / 350 000 · сброс 01.09»), модальность (кастомный listbox),
объём ответа, «Память/персонализация» (модалка с примером + reset к примеру).
Круговой индикатор у «Отправить» удалён; строка квоты под инпутом.
### 25.4 Ошибки ИИ
В пузыре — дружелюбный текст + «Повторить»; raw-ошибка в логи.
403 geo-block — инфраструктурно: OPENROUTER_PROXY_URL (§18.2.1 ранбука).
### 25.5 Стиль
Тёмная тема — допустимый «кабинетный» вариант этого экрана, но все кнопки/
пузыри solid (без градиентов); светлая тема — паритет токенов §17.
Транскрибация — кнопка в шапке (сохраняем).
### 25.6 v2
расход токенов на сообщение, drag чатов в папки, шорткаты «/» над инпутом.
### 25.7 Визуал ИИ-чата (единый язык с §21, тёмная и светлая темы)
- Композер: ОДИН контейнер radius 16, border --line, bg --surface;
  внутри слева icon-кнопка 📎 (hover --surface-2), инпут прозрачный
  (placeholder «Спросите о клиенте, сне, сессии…»), справа круглая кнопка
  40px solid --primary с иконкой отправки (disabled — opacity .4).
  Enter — отправить, Shift+Enter — перенос. Градиентов нет.
- Пузыри: пользователь — solid --primary, белый текст, справа, radius 16
  (угол 4); ИИ — --surface + border --line, слева, иконка бота 24px;
  markdown/таблицы сохраняются; у пользователя время + ✓ как в §21.
- Empty-state: подсказки-вопросы — outline-чипы (hover --surface).
- Сайдбар: «Новый чат» solid --primary; нативные селекты папок из строк
  убрать — иконка папки по hover → меню «В папку…»; папки — строка с
  chevron и счётчиком чатов.
- Селект клиента: кастомный listbox как «Выбрать пациента» в мессенджере
  (клик → дропдаун с поиском и списком); выбор клиента включает тумблер
  снов и обновляет чипы шапки; значение уходит в запрос (clientId +
  clientModeEnabled, §19.4 ранбука). Приёмка: выбрал клиента → чип
  «Клиент: X» → вопрос тянет данные клиента; тумблер снов активен.
- Квота под композером: «Доступно 350 000 токенов · сброс 01.09.2026»,
  12px --text-muted, справа.
  ### 25.8 Папки и список чатов (сайдбар ИИ)
- В строках НЕТ native select и эмодзи 📁. Иконки — только lucide, 16px,
  --text-muted: Folder, FolderOpen, ChevronRight, MessageSquare,
  FolderInput, Plus, X, MoreHorizontal.
- «Новый чат» — solid --primary (градиент убрать окончательно, он всё ещё
  на скринах).
- Строка чата: 40px, radius 10, hover --surface, active --brand-soft;
  имя truncate; действия ПО HOVER: [FolderInput «В папку…»] и [X].
- «В папку…» — dropdown-меню: список папок + «Без папки». Не select,
  не постоянно видимый контрол.
- Строка папки: ChevronRight (поворот при раскрытии) + Folder + имя +
  счётчик чатов (12px --text-muted); по hover: [Plus — чат в эту папку]
  и [⋯ — переименовать / удалить].
- Раскрытая папка: чаты с отступом 16px; пустая — «Нет чатов» muted.
- Удаление папки — confirm; чаты НЕ удаляются, переходят в «Без папки».
- Rename папки — dbl-click по имени или через ⋯.
- Глобальную кнопку «+ Чат в папке» под папками УБРАТЬ (её заменяет Plus
  в строке папки).
- «+ Папка» — inline-инпут имени в сайдбаре, не prompt().
- Данные те же: folders API и folderId чата (ранбук §20.5), backend не менять.
- v2: drag&drop чатов в папки.