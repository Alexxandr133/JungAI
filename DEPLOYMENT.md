# Инструкция по деплою на продакшен сервер

## 🚀 Быстрый старт (автоматический деплой)

### Для Linux/Mac:
```bash
# 1. Создайте .env файл в backend/
cp backend/.env.example backend/.env  # Если есть пример
# Или создайте вручную (см. раздел "Настройка переменных окружения")

# 2. Запустите скрипт деплоя
chmod +x deploy.sh
./deploy.sh
```

### Для Windows:
```cmd
REM 1. Создайте .env файл в backend\ (см. раздел ниже)

REM 2. Запустите скрипт деплоя
deploy.bat
```

Скрипт автоматически:
- ✅ Проверит Node.js версию
- ✅ Установит зависимости
- ✅ Соберет backend и frontend
- ✅ Применит миграции БД
- ✅ Запустит приложение через PM2
- ✅ Настроит автозапуск (опционально)

---

## Подготовка к деплою

### 1. Очистка базы данных

Перед деплоем необходимо очистить все тестовые данные из базы данных:

```bash
cd backend
npm run reset-db
```

Этот скрипт удалит все данные из БД, но сохранит структуру таблиц.

### 2. Настройка переменных окружения

Создайте файл `.env` в директории `backend/` со следующими переменными:

**Генерация безопасного JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

```env
# База данных
DATABASE_URL="file:./prisma/prod.db"

# JWT секрет (ОБЯЗАТЕЛЬНО сгенерируйте безопасный ключ!)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Hugging Face токен для AI (опционально)
HF_TOKEN="your-huggingface-token"

# DeepSeek API ключ (опционально)
DEEPSEEK_API_KEY="your-deepseek-api-key"

# Порт сервера
PORT=4000

# Окружение
NODE_ENV=production

# CORS (настройте под ваш домен)
# Для множественных доменов: CORS_ORIGIN="https://domain1.com,https://domain2.com"
CORS_ORIGIN="https://yourdomain.com"
CORS_CREDENTIALS=true

# Frontend URL (для генерации ссылок)
FRONTEND_URL="https://yourdomain.com"

# Agora (для видеозвонков, опционально)
AGORA_APP_ID="your-agora-app-id"
AGORA_APP_CERTIFICATE="your-agora-certificate"

# Временный токен Agora (для тестирования)
# AGORA_TEMP_TOKEN=""
```

**⚠️ ВАЖНО:** 
- Никогда не коммитьте `.env` файл в git!
- Используйте сильные случайные значения для `JWT_SECRET`
- Настройте `CORS_ORIGIN` на ваш продакшен домен

### 3. Сборка проекта

#### Backend:

```bash
cd backend
npm install
npm run build
npm run prisma:generate
npm run prisma:migrate
```

#### Frontend:

```bash
cd frontend
npm install
npm run build
```

Артефакты сборки будут в `frontend/dist/`

## Деплой на сервер

### Вариант 1: Деплой через SSH

1. **Подключитесь к серверу:**
   ```bash
   ssh user@your-server.com
   ```

2. **Создайте директорию проекта:**
   ```bash
   mkdir -p /var/www/jingai
   cd /var/www/jingai
   ```

3. **Клонируйте репозиторий (или загрузите файлы):**
   ```bash
   git clone https://your-repo-url.git .
   # или загрузите файлы через scp/sftp
   ```

4. **Установите зависимости и соберите проект:**
   ```bash
   # Backend
   cd backend
   npm install --production
   npm run build
   npm run prisma:generate
   npm run prisma:migrate
   
   # Frontend
   cd ../frontend
   npm install
   npm run build
   ```

5. **Настройте переменные окружения:**
   ```bash
   cd ../backend
   nano .env
   # Заполните все необходимые переменные
   ```

6. **Очистите базу данных (если нужно):**
   ```bash
   npm run reset-db
   ```

### Вариант 2: Использование PM2 для управления процессом

1. **Установите PM2:**
   ```bash
   npm install -g pm2
   ```

2. **Используйте готовый `ecosystem.config.js` в корне проекта** (уже создан)

3. **Запустите приложение:**
   ```bash
   pm2 start ecosystem.config.js
   pm2 save
   pm2 startup  # для автозапуска при перезагрузке сервера
   ```
   
   **Или используйте автоматический скрипт деплоя:**
   ```bash
   ./deploy.sh  # Linux/Mac
   # или
   deploy.bat   # Windows
   ```

### Вариант 3: Использование Nginx как reverse proxy

1. **Установите Nginx:**
   ```bash
   sudo apt update
   sudo apt install nginx
   ```

2. **Используйте готовую конфигурацию `nginx.conf.example`** (скопируйте и адаптируйте):
   ```bash
   sudo cp nginx.conf.example /etc/nginx/sites-available/jingai
   sudo nano /etc/nginx/sites-available/jingai
   # Замените yourdomain.com на ваш домен
   ```
   
   Или создайте вручную, используя пример из `nginx.conf.example`

3. **Активируйте конфигурацию:**
   ```bash
   sudo ln -s /etc/nginx/sites-available/jingai /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl reload nginx
   ```

4. **Настройте SSL (Let's Encrypt):**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d yourdomain.com
   ```

## Тестовые аккаунты

После деплоя тестовые аккаунты можно создать вручную через регистрацию или используя seed скрипт (только для разработки).

**Данные для входа (если аккаунты созданы):**
- **Психолог:** `psy@example.com` / `demo`
- **Клиент:** `client@example.com` / `demo`
- **Исследователь:** `res@example.com` / `demo`
- **Админ:** `admin@example.com` / `demo`

⚠️ **В продакшене рекомендуется:**
- Не создавать тестовые аккаунты с простыми паролями
- Использовать реальные email адреса
- Установить сложные пароли для всех аккаунтов

## Проверка работоспособности

1. **Проверьте, что backend запущен:**
   ```bash
   curl http://localhost:4000/api/health
   # или
   pm2 status
   ```

2. **Проверьте логи:**
   ```bash
   pm2 logs jingai-backend
   # или
   tail -f /var/www/jingai/logs/backend-out.log
   ```

3. **Проверьте базу данных:**
   ```bash
   cd backend
   npm run prisma:studio
   # Откроется веб-интерфейс для просмотра БД
   ```

## Обновление приложения

1. **Остановите приложение:**
   ```bash
   pm2 stop jingai-backend
   ```

2. **Обновите код:**
   ```bash
   git pull
   # или загрузите новые файлы
   ```

3. **Пересоберите проект:**
   ```bash
   cd backend
   npm install
   npm run build
   npm run prisma:generate
   npm run prisma:migrate
   
   cd ../frontend
   npm install
   npm run build
   ```

4. **Запустите приложение:**
   ```bash
   pm2 restart jingai-backend
   ```

## Резервное копирование

Рекомендуется настроить автоматическое резервное копирование базы данных:

```bash
# Создайте скрипт backup.sh
#!/bin/bash
BACKUP_DIR="/var/backups/jingai"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp /var/www/jingai/backend/prisma/prod.db $BACKUP_DIR/prod_$DATE.db
# Удалите старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "*.db" -mtime +30 -delete
```

Добавьте в crontab:
```bash
0 2 * * * /path/to/backup.sh
```

## Мониторинг

Рекомендуется настроить мониторинг:
- PM2 Monitoring: `pm2 plus`
- Логирование ошибок
- Мониторинг использования ресурсов
- Алерты при падении сервиса

## Безопасность

- ✅ Используйте HTTPS (SSL/TLS)
- ✅ Настройте firewall (откройте только необходимые порты)
- ✅ Регулярно обновляйте зависимости: `npm audit fix`
- ✅ Используйте сильные пароли для всех аккаунтов
- ✅ Ограничьте доступ к серверу по SSH ключам
- ✅ Настройте rate limiting в Nginx
- ✅ Регулярно делайте резервные копии БД

## Troubleshooting

### Проблема: Приложение не запускается

1. Проверьте логи: `pm2 logs jingai-backend`
2. Проверьте переменные окружения в `.env`
3. Убедитесь, что порт 4000 свободен: `lsof -i :4000`
4. Проверьте права доступа к файлам БД

### Проблема: База данных не работает

1. Проверьте путь к БД в `DATABASE_URL`
2. Убедитесь, что директория существует и доступна для записи
3. Запустите миграции: `npm run prisma:migrate`

### Проблема: Frontend не загружается

1. Проверьте, что файлы собраны в `frontend/dist/`
2. Проверьте конфигурацию Nginx
3. Проверьте права доступа к файлам: `chmod -R 755 frontend/dist`


